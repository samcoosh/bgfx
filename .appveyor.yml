image:
  - Visual Studio 2019
    
environment:
  matrix:
  - PLATFORM: x64
    CONFIG: Release
    TOOLS:
  - PLATFORM: x64
    CONFIG: Debug
    TOOLS:
  - PLATFORM: x32
    CONFIG: Release  
    TOOLS:
  - PLATFORM: x32
    CONFIG: Debug
    TOOLS:
  
install:  
  - git clone --depth 1 https://github.com/bkaradzic/bx
  - git clone --depth 1 https://github.com/bkaradzic/bimg
  - git clone --depth 1 https://github.com/bkaradzic/bgfx.git

before_build:
- cmd: cd bgfx
- cmd: ..\bx\tools\bin\windows\genie %TOOLS% --platform=%PLATFORM% vs2019
  
build_script:
- cmd: msbuild .build\projects\vs2019\bgfx.sln /t:Clean,Build /p:Configuration=%CONFIG%
- cmd: cd ..
  
#after_test:
#  - cmd: nuget pack Package-%PLATFORM%-%CONFIGURATION%.nuspec -Version %APPVEYOR_BUILD_VERSION%-%PLATFORM%-%CONFIGURATION%
  
#artifacts:
#  - path: '**\*.nupkg'
  
#deploy:
#- provider: Environment
#  name: production